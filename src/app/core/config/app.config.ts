/**
 * API base URL. For production, replace at build time or set via environment.
 * Default: backend when running locally (e.g. Cloud Run URL in prod).
 */
export const API_BASE_URL = 'http://localhost:3000';
